# includes: ['layer:basic']  # if you use any interfaces, add them here
includes:
  - 'layer:basic'
  - 'layer:apt'
  - 'interface:mysql'
options:
  apt:
    packages:
      - hello
repo: https://github.com/studentz/myjujutest

